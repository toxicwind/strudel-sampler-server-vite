# Strudel Sampler Environment Variables

# Path to samples directory
STRUDEL_SAMPLES=${HOME}/strudel-dev/samples

# Server port
PORT=5432

# Cache TTL in milliseconds (1 hour)
CACHE_TTL=3600000

# Max cache size (number of metadata entries)
CACHE_MAX_SIZE=500

# Enable hot reload
HOT_RELOAD=true
